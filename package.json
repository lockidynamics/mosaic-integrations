{
  "name": "@lockidynamics/mosaic-integrations",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "test": "node --test && node scripts/validate-repository.mjs",
    "validate": "node scripts/validate-repository.mjs"
  }
}
